[workspace]
members = ["slotbus"]
resolver = "2"

[workspace.package]
version = "0.1.2"
edition = "2021"
rust-version = "1.75"
authors = ["Justin Maier"]
license = "MIT"
repository = "https://github.com/JustMaier/slotbus"
homepage = "https://github.com/JustMaier/slotbus"
categories = ["network-programming", "concurrency", "os"]
keywords = ["ipc", "shared-memory", "lock-free", "rpc", "low-latency"]

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
postcard = { version = "1", features = ["alloc"] }
shared_memory = "0.12"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
thiserror = "2"
